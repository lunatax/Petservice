<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.cuenca.petservice.com.petservice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IdiomaActivity" />
        <!-- <activity android:name=".SplashScreen" android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"> -->
        <activity
            android:name=".SplashScreen"
            android:theme="@android:style/Theme.Wallpaper.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".servicios" />
        <activity android:name=".Aseo" />
        <activity android:name=".tipomascota" />
        <activity android:name=".Vacunas" />
        <activity android:name=".Desparacitacion" />
        <activity android:name=".Antipulgas" />
        <activity android:name=".consulta" />
        <activity android:name=".Carro"></activity>
    </application>

</manifest>